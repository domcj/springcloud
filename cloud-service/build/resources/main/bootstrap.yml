server:
  port: 8002

spring:
  application:
    name: cloud-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: cloud-config
#      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/

management:
  security:
    enabled: false